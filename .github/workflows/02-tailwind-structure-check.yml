name: 02 Tailwind Structure Check

on:
  pull_request:
  push:
    branches-ignore:
      - main

jobs:
  tailwind-structure:
    name: Validate Tailwind Structure
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify Tailwind config exists
        run: |
          if [ ! -f "tailwind.config.js" ] && [ ! -f "tailwind.config.cjs" ]; then
            echo "❌ Tailwind config file not found."
            exit 1
          fi

      - name: Verify input CSS exists
        run: |
          if [ ! -f "src/app/static/src/input.css" ]; then
            echo "❌ Tailwind input CSS file missing: src/app/static/src/input.css"
            exit 1
          fi

      - name: Verify output CSS exists
        run: |
          if [ ! -f "src/app/static/css/tailwind.css" ]; then
            echo "❌ Tailwind output CSS missing: src/app/static/css/tailwind.css"
            echo "Run the staff-only Tailwind build workflow to regenerate it."
            exit 1
          fi

      - name: Verify static folder structure
        run: |
          if [ ! -d "src/app/static/css" ] || [ ! -d "src/app/static/src" ]; then
            echo "❌ Expected src/app/static/css and src/app/static/src directories missing."
            exit 1
          fi

      - name: Tailwind structure check passed
        run: echo "✔ Tailwind structure is valid."
