[allowlist]
description = "Allow known safe files in the template"
paths = [
  "tests/",
  "docs/",
  ".env.example",
  "README.md"
]

regexes = [
  # Allow placeholder secrets like "YOUR_SECRET_HERE"
  '''YOUR_.*_HERE''',

  # Allow hex strings used in examples or docs
  '''[A-Fa-f0-9]{32,}'''
]
